# Reuse from grammar-checker: cp ../grammar-checker/.env .env
# Or add keys here:
ANTHROPIC_API_KEY=
# For daily digest and startup update emails (optional):
RESEND_API_KEY=
DIGEST_FROM_EMAIL=
# Optional: require x-backfill-secret header when calling backfill-cofounder-linkedins
# BACKFILL_SECRET=
